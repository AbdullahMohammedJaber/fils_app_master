import 'package:flutter/material.dart';

class AppConstants {
  static const dynamic splashDelay = 2;
  static const dynamic sliderAnimationTime = 300;
}

class ConstantGlobalVar {
  static const en = Locale('en', '');
  static Locale? locale;
  static const ar = Locale('ar', '');
}
